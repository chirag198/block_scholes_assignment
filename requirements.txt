# Block Scholes Assignment: Daily Closing Prices Retrieval

This project retrieves daily closing prices for the BTC-USD pair from OKX’s public REST API.

## Setup
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt

requests

